import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    list: {
        margin: 10,
        backgroundColor: '#144552',
        alignItems: 'center',
        borderRadius: 10,
        marginBottom: 30
    }
})
